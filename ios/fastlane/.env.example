# iOS Fastlane env example (DO NOT COMMIT real secrets)
# Copy to: ios/fastlane/.env  (gitignored)

# --- App Store Connect API Key ---
# Create in App Store Connect -> Users and Access -> Integrations -> App Store Connect API
ASC_KEY_ID=
ASC_ISSUER_ID=
# Base64 of the downloaded AuthKey_XXXXXX.p8 file content (single line)
ASC_KEY_P8_BASE64=

# --- App Identifiers ---
APP_BUNDLE_ID=ai.papertok.paperreader

# --- Apple ID (for match, only needed when USE_MATCH=true or when auto-detected) ---
FASTLANE_APPLE_ID=

# --- match (recommended) ---
# Password to encrypt/decrypt the match repo
MATCH_PASSWORD=
# GitHub PAT basic auth (base64 of "username:token")
# Example: echo -n "ViffyGwaanl:ghp_xxx" | base64
MATCH_GIT_BASIC_AUTHORIZATION=

# Optional: skip match on a local machine that already has signing set up in Xcode
# USE_MATCH=false
